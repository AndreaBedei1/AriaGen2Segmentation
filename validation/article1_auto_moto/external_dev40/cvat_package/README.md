# External dev40 CVAT package

20 real car frames are populated; 20 motorcycle slots remain blocked. Import `images/` as an image task and create masks/polygons using `labels.json`. No generated mask is ground truth. Export CVAT for images 1.1 with masks, retain void/ignore, then run the conversion validator.
