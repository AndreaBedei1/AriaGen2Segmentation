# Genuinely debatable

Frames where the models disagree or are uncertain. Annotate what you can defend from the image and tick `ambiguity_flag`; these are the frames whose disagreement is worth recording.

No frame in the current selection matches this category. That is a fact about the selection, not a placeholder: the category is kept so it is filled the moment such a frame is selected.
